---
import Layout from "./layouts/Layout.astro";

const explainer = `
Welcome to the edge of the Net.
This is where chrome meets chaos, where stories are lived, not watched, and mods do not just change games, they rewrite reality.

I am Starboy Chad.
I build immersive, heavily modded Cyberpunk 2077 and Fallout worlds.
I document the descent.
And soon, I ship the tech that makes it possible.

Jack in.
`;
---

<Layout title="StarboyChad.com">
  <section class="card">
    <p class="kicker">ACCESS POINT</p>
    <h1 class="title">Starboy Chad // Neon Wasteland Operations</h1>

    {explainer.split("\n").filter(Boolean).map((line) => (
      <p class="body">{line}</p>
    ))}

    <div class="buttons">
      <a class="btn cyan" href="/channels">Watch</a>
      <a class="btn pink" href="/modding">Mods</a>
    </div>
  </section>

  <style>
    .card{
      border:1px solid rgba(255,255,255,0.12);
      background: rgba(255,255,255,0.04);
      border-radius: 22px;
      padding: 28px;
      backdrop-filter: blur(10px);
      box-shadow: 0 0 0 1px rgba(255,255,255,0.06);
    }
    .kicker{ font-size:11px; letter-spacing:0.35em; opacity:0.7; text-transform:uppercase; margin:0 0 10px; }
    .title{ margin:0 0 18px; font-size: 30px; line-height:1.15; }
    .body{ font-size: 18px; line-height:1.6; opacity:0.9; margin: 0 0 10px; }
    .buttons{ display:flex; gap:10px; flex-wrap:wrap; margin-top: 16px; }
    .btn{
      display:inline-block; padding:10px 16px; border-radius: 999px;
      text-decoration:none; color:#fff; font-size:12px; letter-spacing:0.22em; text-transform:uppercase;
      border:1px solid rgba(255,255,255,0.18);
      background: rgba(255,255,255,0.05);
    }
    .btn:hover{ background: rgba(255,255,255,0.10); }
    .cyan{ border-color: rgba(34,211,238,0.35); box-shadow: 0 0 18px rgba(34,211,238,0.16); }
    .pink{ border-color: rgba(232,121,249,0.35); box-shadow: 0 0 18px rgba(232,121,249,0.14); }
  </style>
</Layout>
